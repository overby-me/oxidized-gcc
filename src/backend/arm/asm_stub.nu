#!/usr/bin/env nu
# Placeholder assembler for AArch64 backend.
# Set MY_ASM to point to this script to test the custom assembler integration.
# TODO: Replace this stub with a real assembler implementation.
#
# Runs as a top-level script (no `main`) so it accepts an assembler's
# arbitrary dash-prefixed arguments without nushell trying to parse them
# as its own flags. It always fails, signalling the stub is not implemented.
print -e "ERROR: AArch64 custom assembler stub called but not yet implemented."
exit 1
